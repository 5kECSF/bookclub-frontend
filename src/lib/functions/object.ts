export const isEmptyObject = (obj: Record<string, any>): boolean => {
  return Object.keys(obj).length === 0;
};

//
// export const ReturnErrors = (obj: Record<string, any>): => {
//   // return Object.keys(obj).map(ite)
// };
